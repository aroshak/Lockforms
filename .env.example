# Database
DATABASE_URL="postgresql://user:password@localhost:5432/lockforms?schema=public"

# Admin Authentication
# Generate with: echo -n "your_password" | openssl dgst -sha256 | cut -d' ' -f2
ADMIN_PASSWORD_HASH=""

# License Key (Base64 encoded license file content)
LICENSE_KEY=""

# Security Settings
NODE_ENV="development"

# Sharing & Embedding
# Base URL for generating share and embed links
NEXT_PUBLIC_APP_URL="http://localhost:3000"
